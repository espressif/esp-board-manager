# Devices Configuration for M5Stack AtomS3U
#
# Source: M5Stack AtomS3U documentation
# Reference: https://docs.m5stack.com/en/core/AtomS3U
#
# There is no audio_codec device here on purpose: the SPM1423 microphone
# has no control interface, so the pdm-in peripheral in
# board_peripherals.yaml is the whole story.
#
devices:
  # ---------- On-board WS2812 addressable RGB LED ----------
  - name: led_strip
    chip: ws2812
    type: led_strip
    sub_type: rmt
    version: 1.0.0
    config:
      strip_gpio_num: 35
      max_leds: 1
      led_model: LED_MODEL_WS2812
      color_component_format: LED_STRIP_COLOR_COMPONENT_FMT_GRB
      invert_out: false
      rmt:
        clk_src: RMT_CLK_SRC_DEFAULT
        resolution_hz: 10000000
        mem_block_symbols: 0
        with_dma: false

  # ---------- Side button (GPIO41, active-low) ----------
  - name: user_button
    type: button
    sub_type: gpio
    version: default
    config:
      active_level: 0
      enable_power_save: false
      disable_pull: false
      long_press_time: 2000
      short_press_time: 100
    peripherals:
      - gpio_name: gpio_user_button

  # ---------- Infrared transmit LED (GPIO12) ----------
  - name: ir_tx
    type: gpio_ctrl
    version: default
    config:
      active_level: 1
      default_level: 0
    peripherals:
      - gpio_name: gpio_ir_tx
